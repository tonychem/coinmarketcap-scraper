elasticsearch:
  scheme: "${ELASTICSEARCH_SCHEME:http}"
  host: "${ELASTICSEARCH_HOST:localhost}"
  port: "${ELASTICSEARCH_PORT:9200}"

tomcat:
  host: "${TOMCAT_HOST:localhost}"
  port: "${TOMCAT_PORT:8080}"

credentials:
  - "<CREDENTIAL1>"
  - "<CREDENTIAL2>"

task:
  symbols:
    - "<SYMBOL1>"
    - "<SYMBOL2>"
    - "<SYMBOL3>"
    - "<SYMBOL4>"
    - "<SYMBOL5>"