elasticsearch:
  scheme: "${DOCKER_ENV:http}"
  host: "${DOCKER_ENV:localhost}"
  port: "${DOCKER_ENV:9200}"

tomcat:
  host: "${DOCKER_ENV:localhost}"
  port: "${DOCKER_ENV:8080}"

credentials:
  - "<CREDENTIAL1>"
  - "<CREDENTIAL2>"

task:
  symbols:
    - "<SYMBOL1>"
    - "<SYMBOL2>"
    - "<SYMBOL3>"
    - "<SYMBOL4>"
    - "<SYMBOL5>"